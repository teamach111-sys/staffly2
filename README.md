download the zip file or clone
npm install
npm run dev
you can use the .bat files in the folder to switch the themes and colors, bat files only work on windows, on a mac you have to switch this manually:import '../../themes/duplicates/header-dup3.css'; in header.jsx and 
import '../../themes/duplicates/dup3.css'; in home.jsx
switch by renaming dup[number] like dup1, dup2, dup3, dup4
dup0 is original color which is orange
